<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">

    <a class="navbar-brand d-flex align-items-center" href="/">
      <img src="/Asset/images/logo.PNG" alt="Logo" style="height:32px; width:auto; margin-right:8px;">
      PlanSimple
    </a>

    <button class="navbar-toggler" type="button"
      data-bs-toggle="collapse" data-bs-target="#navbarMenu"
      aria-controls="navbarMenu" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarMenu">

      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

        <!-- HOME -->
        <li class="nav-item">
          <a class="nav-link <%= title === 'Home' ? 'active' : '' %>" href="/home">
            <i class="fa-solid fa-house"></i> Home
          </a>
        </li>

        <!-- SCHEDULE -->
        <li class="nav-item">
          <a class="nav-link <%= title === 'Schedule' ? 'active' : '' %>" href="/schedule">
            <i class="fa-solid fa-calendar"></i> Schedule
          </a>
        </li>

        <!-- INFO -->
        <li class="nav-item">
          <a class="nav-link <%= title === 'Info' ? 'active' : '' %>" href="/help">
            <i class="fa-solid fa-info"></i> Info
          </a>
        </li>

        <!-- LOGIN / REGISTER (user NOT logged in) -->
        <% if (!user) { %>

          <li class="nav-item">
            <a class="nav-link <%= title === 'Login' ? 'active' : '' %>" href="/login">
              <i class="fa-regular fa-user"></i> Login
            </a>
          </li>

          <li class="nav-item">
            <a class="nav-link <%= title === 'Register' ? 'active' : '' %>" href="/register">
              <i class="fa-solid fa-right-to-bracket"></i> Register
            </a>
          </li>

        <% } else { %>

        <!-- USER LOGGED IN -->
          <li class="nav-item">
            <span class="nav-link disabled">Hello, <%= user.email %></span>
          </li>

          <li class="nav-item">
            <form action="/logout" method="POST" class="d-inline">
              <button class="nav-link btn btn-link text-white" style="text-decoration:none;">
                Logout
              </button>
            </form>
          </li>

        <% } %>

      </ul>

    </div>
  </div>
</nav>