 <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">

    <a class="navbar-brand d-flex align-items-center" href="/">
  <img src="/Asset/images/logo.PNG" alt="Logo" style="height:32px; width:auto; margin-right:8px;">
  PlanSimple
</a>

    <button class="navbar-toggler" type="button"
        data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"> 
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

        <li class="nav-item">
          <% if (title =="Home") { %>
            <a class="nav-link active" href="/home"><i class="fa-solid fa-house"></i> Home</a>
          <% } else { %>
            <a class="nav-link" href="/home"><i class="fa-solid fa-house"></i> Home</a>
          <% } %>
        </li>

        <li class="nav-item">
          <% if (title =="Schedule") { %>
            <a class="nav-link active" href="/schedule"><i class="fa-solid fa-calendar"></i> Schedule</a>
          <% } else { %>
            <a class="nav-link" href="/schedule"><i class="fa-solid fa-calendar"></i> Schedule</a>
          <% } %>
        </li>

        <li class="nav-item">
          <% if (title =="Info") { %>
            <a class="nav-link active" href="/help"><i class="fa-solid fa-info"></i> Info</a>
          <% } else { %>
            <a class="nav-link" href="/help"><i class="fa-solid fa-info"></i> Info</a>
          <% } %>
        </li>

      </ul>
    </div>

  </div>
</nav>
